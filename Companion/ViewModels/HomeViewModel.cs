using Companion.Services;

namespace Companion.ViewModels
{
    public class HomeViewModel : BaseViewModel
    {
        public HomeViewModel(ApiService apiService) : base(apiService)
        {
        }
    }
}